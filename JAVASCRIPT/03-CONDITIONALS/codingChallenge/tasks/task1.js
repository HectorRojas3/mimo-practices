/* In this challenge, make the program run!
Complete the conditional statement with an appropiate
boolean value so that the conditional body executes and the message is logged
to the console. */ 

        let computerOn = true;
        if (computerOn){
            console.log("Program Running!");
        }